{% extends 'base.html.twig' %}
{% block title %}Hello DefaultController!{% endblock %}

{% set string = "some string \n from \n database" %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name|upper }}! ✅</h1>
    {% set users = [{name: 'John'}, {name: 'Susan'}, {name: 'Rober'}] %}


    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'D:/curs/project/src/Controller/DefaultController.php'|file_link(0) }}">src/Controller/DefaultController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'D:/curs/project/templates/default/index.html.twig'|file_link(0) }}">templates/default/index.html.twig</a></code></li>
    </ul>
{#    {% form_theme form 'bootstrap_4_layout.html.twig' %}#}


    <p>The google tracking code is: {{ga_code}}</p>
    {{render(controller(
      'App\\Controller\\DefaultController::mostPopularPosts',
       {'number': 5}
    ))}}

{#    {{ form_start(form) }}#}
{#    {{ form_label(form.title) }}#}
{#    {{ form_widget(form.title) }}#}
{#    {% if form.created_at is defined %}#}
{#    {{ form_label(form.created_at) }}#}
{#    {{ form_widget(form.created_at) }}#}
{#    {% endif %}#}
{#    {{ form_label(form.file) }}#}
{#    {{ form_widget(form.file) }}#}
{#    {{ form_label(form.agreeTerms) }}#}
{#    {{ form_widget(form.agreeTerms) }}#}
{#    {{ form_label(form.save) }}#}
{#    {{ form_widget(form.save) }}#}
{#    {{ form_end(form) }}#}
{#    {{ form(form) }}#}

    {% if app.user %}
    <a href="{{ path('logout') }}">logout</a>
    {% endif %}

    {% if is_granted('ROLE_ADMIN') %}
        <a href="...">Delete something, only admin can do this</a>
    {% endif %}
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <P>Username: {{ app.user.username }}</P>
    {% endif %}

    <a href="{{ path('login') }}">awsome link</a>

        {{ 'symfony.learning'|transchoice(count) }}


</div>
{% block javascripts %}

<script src="{{asset('build/js/custom.js')}}">
</script>


{% endblock %}

{% endblock %}
